'use babel';

import MinimalTypescriptView from '../lib/minimal-typescript-view';

describe('MinimalTypescriptView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
